DOM Tree
* js操作domTree
  通过dom树，js可以增删改查页面中的所有html元素、可以增删改查页面中所有的html属性，
  可以增删改查页面中所有的css属性，能够对页面中所有的时间做出反应。
  （元素、属性、css Style，事件）
* 查找html元素
  - 通过元素的id属性
  - 通过元素的标签名称
  - 通过元素的类型即元素的class属性
* 